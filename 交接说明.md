# Memori-Vault 项目交接说明

> 这份文档给**接手项目的同学**看。看完你会知道：这是个什么项目、怎么跑起来、
> 以及**最重要的一条规矩——所有开发都在 `collab` 分支上做，别碰 `main` 和 `dev`。**
> 文末有一段**可以直接复制粘贴给你的 AI（Cursor / Claude Code / Codex / Copilot 等）的提示词**。

---

## 一、这是什么项目（一句话）

**Memori-Vault** —— 一个**本地优先、可验证、可审计**的长期记忆引擎（Local-first Verifiable Memory OS Lite）。
说人话：一个能"问你自己的文档、并且告诉你答案到底来自哪份文件哪一段"的本地知识库问答工具。

### 再详细一点

普通 RAG 工具能给你一个"看起来相关"的答案，但回答不了三件事：

1. 这个答案到底来自哪一份文件、哪一个片段？
2. 文档证据不足时，它会不会为了凑答案而编造？
3. agent / 长期对话产生的新记忆，怎么保存、更新、审计，而不污染原始文档证据？

Memori-Vault 就是奔着这三点做的：

- **可验证答案**：每次问答都带 `citations`（引用了哪些文件/chunk）、`evidence`（命中片段和原因）、`metrics`（各阶段耗时）、`failure_class`（失败分类）。
- **Evidence Firewall（证据防火墙）**：长期记忆很有用但也危险——用户偏好、项目上下文、历史对话不能冒充"文档引用"。文档 citation 只能来自文档 chunk，记忆只能进 `memory_context`。
- **分层记忆**：STM（短期）/ MTM（跨会话）/ LTM（长期）/ TKG（时序知识图谱）/ Policy（治理边界）。
- **本地优先**：核心数据存在本机 **SQLite 单文件**，默认不依赖云、不依赖外部向量库，可配本地模型离线跑。
- **中文/CJK 友好**、**官方 MCP 接口**（Claude Code / Codex / OpenCode 可标准接入）。
- 许可证 **Apache 2.0**。当前版本 **v1.5.2**。

---

## 二、技术栈 & 目录结构

**技术栈**：Rust（后端/核心）+ Tauri（桌面壳）+ React 19 + Vite + Tailwind CSS 4（前端）。

Rust workspace 各 crate：

| 目录 | 作用 |
| --- | --- |
| `memori-parser` | Markdown / TXT / PDF / DOCX 解析与分块 |
| `memori-storage` | SQLite schema、文档、FTS、向量、图谱、记忆生命周期 |
| `memori-core` | 查询分析、检索、RRF/gating、Memory Router、Context Composer、Evidence Firewall |
| `memori-desktop` | Tauri 桌面壳、设置持久化、本地模型运行时协调 |
| `memori-server` | Axum HTTP API（`http://127.0.0.1:3757`）、MCP endpoint（`/mcp`） |
| `memori-vault` | 基础 crate |
| `ui/` | React 前端，入口 `App.tsx` |
| `docs/` | 架构、教程、规划、QA 基线等文档 |

想更深入了解架构，读 `docs/architecture/MEMORY_OS_LITE.md` 和 `docs/architecture/STRUCTURE.md`。

---

## 三、怎么跑起来

**前置环境**：Rust 1.85+、Node.js（含 pnpm）、Tauri CLI（`cargo install tauri-cli`）。

### 桌面开发模式（Windows 一键脚本）

```powershell
.\dev.ps1                # 同时启动 服务端 + 桌面（各开一个窗口）
.\dev.ps1 -ServerOnly    # 只启服务端
.\dev.ps1 -DesktopOnly   # 只启桌面
```

### 或者手动启动

```bash
pnpm --dir ui install
pnpm --dir ui run dev -- --host 127.0.0.1 --port 1420 --strictPort
cargo tauri dev -p memori-desktop
```

### 只启动服务端

```bash
cargo run -p memori-server
```

默认服务端 `http://127.0.0.1:3757`，MCP endpoint `http://127.0.0.1:3757/mcp`。

> ⚠️ **Windows 小坑**：本项目所有 `.ps1` 脚本必须存成 **UTF-8 with BOM**，否则 PowerShell 5.1 会按 GBK 读，中文注释乱码、脚本报错。

---

## 四、⭐ 最重要：分支规矩

这个仓库有几条分支：

- `main` —— 稳定发布分支，**别碰**。
- `dev` —— 原作者的主开发分支，**别碰**。
- **`collab` —— 这是给你们的分支，所有开发都在这里做。** ← 你现在就在这条分支上。

**规则很简单：**

1. ✅ 所有改动、提交都在 **`collab`** 分支上。
2. ❌ 不要切换到、不要提交到、不要合并进 `main` 或 `dev`。
3. ❌ 不要 `git push --force`，不要对 `main`/`dev` 做 rebase/merge。
4. 🙋 如果需要从 `main`/`dev` 同步最新代码，先联系原作者，别自己动手合。

`collab` 是从 `dev` 拉出来的，包含了目前**最新的可用代码**（`dev` 比 `main` 领先 38 个提交），所以你拿到的就是最全的版本，放心在上面开发。

---

## 五、Git 极简速查（不熟悉 git 也能用）

```bash
git branch --show-current      # 我现在在哪条分支？（应该永远显示 collab）
git checkout collab            # 万一不在 collab，切回来
git status                     # 看改了哪些文件
git add .                     # 暂存所有改动
git commit -m "写清楚你改了什么"   # 提交
```

如果你们**不用 GitHub**，以上命令在本地就能完整跑，改动都存在你自己电脑的这个文件夹里，不需要联网、不需要账号。只有当你想把代码传给别人时才需要 `git push` / 打包发送。

---

## 六、⭐ 直接复制这段给你的 AI

> 下面整段话可以**原样粘贴**到 Cursor / Claude Code / Codex / GitHub Copilot Chat 等任何 AI 编程助手，
> 作为它在本项目里的"开场规则"。（另外仓库根目录已经放了 `AGENTS.md` 和 `CLAUDE.md`，
> 支持自动读取规则文件的 AI 工具会自动加载，无需每次粘贴。）

```text
你正在协助开发 Memori-Vault 项目——一个 Rust(Tauri)+React 的本地优先可验证记忆引擎。

【最高优先级、不可违反的规则】
1. 本项目所有开发只在 `collab` 分支上进行。
2. 你在执行任何 git 操作或写文件之前，必须先运行 `git branch --show-current` 确认当前分支是 `collab`；
   如果不是，先执行 `git checkout collab` 切回来，再继续。
3. 绝对禁止 checkout、commit、merge、rebase、push 到 `main` 或 `dev` 分支。
4. 绝对禁止 `git push --force`，绝对禁止对 `main`/`dev` 做任何形式的合并或变基。
5. 如果某个任务看起来需要动 `main` 或 `dev`（例如同步上游更新），不要自己动手，
   停下来告诉我，让我（人类）来处理。

【工作方式】
- 每次改动后用清晰的中文 commit message 说明改了什么。
- 遇到 .ps1 脚本，保存为 UTF-8 with BOM（否则 Windows PowerShell 5.1 会中文乱码）。
- 不确定的地方先问我，别擅自改动 main/dev 或删除数据。

请确认你已理解以上规则，然后再开始任务。
```

---

有任何搞不懂的，先看 `README.md`、`docs/` 目录，或直接问原作者。祝开发顺利 🚀
