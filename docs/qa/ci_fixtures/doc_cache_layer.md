# 缓存层 CACHE-31 运行手册

## 概述
本手册描述 缓存层 CACHE-31 的运行约束与责任划分，涉及 穿透 雪崩 预热 等相邻主题但以本模块为准。

## 关键约束
经评审确认，CACHE-31 的热点 key 淘汰策略采用 LFU 而非 LRU。该约束为硬性要求，变更需走审批流程。

## 责任与运维
缓存层 CACHE-31 由平台组负责，相关 穿透 雪崩 预热 流程在其它手册另行说明，此处不展开。
